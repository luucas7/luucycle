interface:
  display_name: "luucycle"
  short_description: "Route explicit multi-agent workflows"
  default_prompt: "Use $luucycle to audit my setup and recommend the next command."
policy:
  allow_implicit_invocation: false
