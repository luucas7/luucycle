# luucycle Roster

Single source of truth for available workers. You may update this file mid-process if you discover missing CLI arguments (e.g., via `--help`). Toggle `Accessible: true|false` instead of deleting models. Agent facts only - role → agent mapping lives in `ROLES.md`.

## Agents

_None yet._ An empty roster is the default state: every CLI and model is user-specific, so nothing ships hardcoded. Populate it with the `add-cli` branch (`ADD-CLI.md`) - propose, confirm, append, map into `ROLES.md`. An empty roster is blocking (RULES rule 9): never dispatch on one. Entries are append-only - once added, toggle `Accessible: true|false` rather than deleting.
